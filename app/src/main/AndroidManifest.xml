<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.zippy">

    <application
        android:allowBackup="false"
        android:background="@android:color/white"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zippy"
        tools:ignore="AllowBackup">
        <activity
            android:name=".SelfInstructorProfileActivity"
            android:label="@string/title_activity_main2"></activity>
        <activity android:name=".ChangeProfilePictureActivity" />
        <activity android:name=".SelfStudentProfileActivity" />
        <activity android:name=".ChooseAccountType" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>